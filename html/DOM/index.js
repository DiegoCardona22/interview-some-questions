// El DOM es la representación del documento HTML en forma de árbol de nodos.
// Cada nodo es un objeto y representa una parte del documento (ej: elemento, texto, atributo, comentario, etc).

// Ejemplo de DOM
const title = document.getElementById('title');
title.innerHTML = 'DOMMMM';